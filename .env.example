# Compose interpolates these into compose.yml at `docker compose up` time.
# Copy this file to `.env` (which is gitignored) and fill in real values.

POSTGRES_USER=user_role
POSTGRES_PASSWORD=change_me
POSTGRES_DB=volt
